export default function Dashboard() {
    return (
        <div className="p-8">
            <h1 className="text-2xl font-semibold">Cost Dashboard</h1>
            <p className="text-muted-foreground">Connect to backend to see data.</p>
        </div>
    );
}